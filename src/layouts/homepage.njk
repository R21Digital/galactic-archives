<!DOCTYPE html>
<html lang="en">
  <head>
    {% include "head.njk" %}
  </head>
  <body>
    <div class="layout">
      {% include "sidebar.njk" %}
      <main class="content">
        <h1>Galactic Archives</h1>
        <p>Welcome to the player-powered knowledge base for <strong>Star Wars Galaxies</strong>.</p>

        <h2>Explore Categories</h2>
        <ul class="category-grid">
          {% set cats = [] %}
          {% for page in collections.all %}
            {% set cat = page.data.category %}
            {% if cat and page.data.eleventyExcludeFromCollections != true and not (cat in cats) %}
              {% set _ = cats.push(cat) %}
            {% endif %}
          {% endfor %}
          {% for cat in cats | sort %}
            <li>
              <!-- Use the slugified category for the URL -->
              <a href="/{{ cat | categorySlug }}/">{{ cat }}</a>
            </li>
          {% endfor %}
        </ul>
      </main>
    </div>
    {% include "footer.njk" %}
  </body>
</html>
