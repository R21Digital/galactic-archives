<!DOCTYPE html>
<html lang="en">
  <head>
    {% include "head.njk" %}
  </head>
  <body>
    <div class="layout">
      {% include "sidebar.njk" %}
      <main class="content">
        <h1>{{ title }}</h1>
        <p><strong>Category:</strong> {{ category }} | <strong>Tags:</strong> {{ tags | join(', ') }}</p>
        <p><em>Last updated: {{ last_updated }}</em></p>
        <article>
          {{ content | safe }}
        </article>
      </main>
    </div>
  </body>
</html>
